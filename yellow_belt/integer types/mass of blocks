int main() {
	int numberOfBlocks,volume;
	cin >> numberOfBlocks>>volume;
	unsigned long long int sum = 0;
	for (int i = 0; i < numberOfBlocks; i++) {
		unsigned long long int temp1,temp2,temp3;
		cin >> temp1>>temp2>>temp3;
		sum += temp1 * temp2 * temp3 * volume;
	}
	cout << sum;
}
